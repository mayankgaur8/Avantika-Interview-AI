# ───── Server ─────
PORT=3001
NODE_ENV=development

# ───── Database (PostgreSQL) ─────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=ai_interview_bot

# ───── Redis ─────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ───── JWT ─────
JWT_SECRET=change-me-in-production-use-a-32char-random-string
JWT_EXPIRES_IN=8h
JWT_REFRESH_SECRET=change-me-refresh-secret-32chars
JWT_REFRESH_EXPIRES_IN=7d

# ───── OpenAI ─────
OPENAI_API_KEY=sk-your-openai-key-here
OPENAI_MODEL=gpt-4o

# ───── Judge0 (Code Sandbox) ─────
JUDGE0_API_URL=http://localhost:2358
JUDGE0_API_KEY=

# ───── Rate Limiting ─────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# ───── CORS ─────
CORS_ORIGIN=http://localhost:3000

# ───── Mail (SMTP) ─────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-gmail@gmail.com
MAIL_PASS=your-app-password
MAIL_FROM=AI Interview Bot <noreply@aiinterviewbot.dev>
